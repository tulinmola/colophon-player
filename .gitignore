.idea/
node_modules/
*.map
.DS_Store
.sourcemaps
dist/
.env.*.local

# Amstrad's firmware images (see tools/fetch-roms.sh). Distributable with
# emulators by Amstrad's permission, but fetched rather than committed.
src/public/roms/
