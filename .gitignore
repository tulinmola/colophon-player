.idea/
node_modules/
*.map
.DS_Store
.sourcemaps
dist/
.env.*.local

# The machines' firmware images, fetched by tools/fetch_roms.js rather than
# committed. What may be distributed with an emulator, and on whose
# permission, is recorded in the emulator's own fetch script.
src/public/roms/
test-results/
playwright-report/
